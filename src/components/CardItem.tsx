import React from 'react';

interface ICardItem {
    icon?: string;
    title: string;
    href: string;
}

export function CardItem(props: ICardItem){
    const { icon, title, href } = props;
    return (
        <div>
            
        </div>
    )
}